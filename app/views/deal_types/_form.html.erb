<%= form_for @deal_type, :html => { :class => "form-horizontal deal_type" } do |f| %>

    <% if @deal_type.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@deal_type.errors.count, "error") %> prohibited this deal_type from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @deal_type.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :deal_type_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :deal_type_name, :class => 'form-control' %>
    </div>
    <%= error_span(@deal_type[:deal_type_name]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            deal_types_path, :class => 'btn btn-default' %>

<% end %>
