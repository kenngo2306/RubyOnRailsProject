<%= form_for @requested_sales_document, :html => { :class => "form-horizontal requested_sales_document" } do |f| %>

    <% if @requested_sales_document.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@requested_sales_document.errors.count, "error") %> prohibited this requested_sales_document from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @requested_sales_document.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :requested_sales_document_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :requested_sales_document_name, :class => 'form-control' %>
    </div>
    <%= error_span(@requested_sales_document[:requested_sales_document_name]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            requested_sales_documents_path, :class => 'btn btn-default' %>

<% end %>
