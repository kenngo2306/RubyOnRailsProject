<%= form_for @irrigation_responsibility, :html => { :class => "form-horizontal irrigation_responsibility" } do |f| %>

    <% if @irrigation_responsibility.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@irrigation_responsibility.errors.count, "error") %> prohibited this irrigation_responsibility from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @irrigation_responsibility.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :irrigation_responsibility_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :irrigation_responsibility_name, :class => 'form-control' %>
    </div>
    <%= error_span(@irrigation_responsibility[:irrigation_responsibility_name]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            irrigation_responsibilities_path, :class => 'btn btn-default' %>

<% end %>
