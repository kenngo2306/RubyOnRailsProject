<%= form_for @category_type, :html => { :class => "form-horizontal category_type" } do |f| %>

    <% if @category_type.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@category_type.errors.count, "error") %> prohibited this category_type from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @category_type.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :category_type_name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :category_type_name, :class => 'form-control' %>
    </div>
    <%= error_span(@category_type[:category_type_name]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            category_types_path, :class => 'btn btn-default' %>

<% end %>
